package router

import (
	. "github.com/onsi/ginkgo/v2"
	. "github.com/onsi/gomega"
)

var _ = Describe("[router-testing] router-tests", func() {
	It("should support passing tests", func() {
		Expect(true).To(BeTrue())
	})
})

